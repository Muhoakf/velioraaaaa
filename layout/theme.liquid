<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>{{ shop.name }}</title>
    <link href="{{ 'theme.css' | asset_url }}" rel="stylesheet" type="text/css" media="all" />
  </head>
  <body>
    {% section 'main' %}
  </body>
</html>
